name: etcd

on:
  push:
    branches: [ main ]
  pull_request:
    branches:
      - main
      - feature-etcdclient

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - uses: addnab/docker-run-action@v3
        with:
          username: zfkhhh
          password: zfk857213
          image: quay.io/coreos/etcd:latest
          options: -p 2379:2379
          run: echo `curl http://127.0.0.1:2379/version`