name: etcd

on:
  push:
    branches: [ main ]
  pull_request:
    branches:
      - main
      - feature-etcdclient

jobs:
  build:
    runs-on: ubuntu-latest
    services:
      etcd:
        image: k8s.gcr.io/etcd:3.5.0-0
        ports:
          - 2379:2379